# pnpm セキュリティ設定

# インストール時にパッケージのライフサイクルスクリプト（postinstall等）を自動実行しない
# サプライチェーン攻撃の防止: 悪意のあるスクリプトの自動実行を阻止
ignore-scripts=true

# 厳格なピアディペンデンシー解決
# 依存関係の不整合を早期に検出
strict-peer-dependencies=true

# pnpm の厳格モード: package.json に宣言されていないパッケージへのアクセスを禁止
# ファントムディペンデンシーを防止
node-linker=hoisted
